Create directories
* uploads
* tmp
here and set writing permissions to them.

Optionally create
* local_settings.py
to overwrite default settings.

To start webserver run the following from the current directory:
python manage.py runserver --adminmedia=../../externals/apps/grappelli/media
