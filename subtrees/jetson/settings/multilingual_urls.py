# list of paths not to redirect to language-specific-prefixed pages
PATHS_NO_REDIRECTION = (
    MEDIA_URL,
    UPLOADS_URL,
    JETSON_MEDIA_URL,
    "/admin/",
    ADMIN_MEDIA_PREFIX,
    "/recrop/cropping-preview/",
    "/helper/",
)
