{% load base_tags image_modifications %}
{% with object as exhibition %}
    <div class="item col-xs-12 col-sm-12 col-md-12 col-lg-12 ">
        <a href="/de/ausstellungen/{{ exhibition.slug }}/">
            <h4>
                {% if exhibition.permanent %}
                    Dauerausstellung{% if exhibition.start %}: <span>{{ exhibition.start|date:"d.m.Y" }} bis auf Weiteres</span>{% endif %}
                {% else %}
                    <span>{{ exhibition.start|date:"d.m" }} – {{ exhibition.end|date:"d.m.Y" }}</span>
                {% endif %}
            </h4>
            <h3>{{ exhibition.title_de|safe }}</h3>
            {% if exhibition.subtitle_de %}
                <h4>{{ exhibition.subtitle_de|safe }}</h4>
            {% endif %}
            {% if exhibition.location_name %}
                <h4>{{ exhibition.location_name|safe }}</h4>
            {% else %}
                {% if exhibition.museum %}
                    <h4>{{ exhibition.museum.title_de|safe }}</h4>
                {% endif %}
            {% endif %}
        </a>
    </div>
{% endwith %}
